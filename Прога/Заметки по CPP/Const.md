```C++
int a;
const int * p1= &a; // указатель на константу;
int const p2 =&a   // Указатель на константу
p1=20  // ошибка
p2=0 	 // Нормально
// константный указатель
int const p3=&ap3=30; // Норм
p3=0 	// ошибка
//Константный указатель на константу
int const * const p4 = &a;
p4= 30 // ошибка
p4=0 	//ошибка
// слово const делает неизменяемым тип слева от него
//
/----------------------------------------/
int & size(){
} //вернет ссылку. можно поменять значение
v.size()=20; //Можно поменять ссылку.
/----------------------------------------/
//  Ключевое слово mutable позвляет определять поля, которые можно изменять
//внутри константных методов.
struct IntArray{
private:
int a;
mutable int b; // это значение можно менять из константных методов
}
/----------------------------------------*/
```